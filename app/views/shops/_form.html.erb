<%= simple_form_for @shop do |f| %>

	<%= f.input :name %>
	<%= f.input :address %>
	<!-- <%= f.input :latitude %> -->
	<!-- <%= f.input :longitude %> -->
	<%= f.input :colour %>

	<%= f.button :submit %>

<% end %>